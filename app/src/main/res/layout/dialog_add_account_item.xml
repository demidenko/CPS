<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingVertical="5dp"
    android:paddingHorizontal="5dp"
    >

    <ImageView
        android:id="@+id/dialog_add_account_item_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_add_box"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/dialog_add_account_item_help"
        app:layout_constraintBottom_toBottomOf="@id/dialog_add_account_item_help"
        android:layout_margin="3dp"
        />

    <TextView
        android:id="@+id/dialog_add_account_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="title"
        style="@style/AddAccountItem"
        app:layout_constraintStart_toEndOf="@id/dialog_add_account_item_img"
        app:layout_constraintTop_toTopOf="@id/dialog_add_account_item_help"
        app:layout_constraintBottom_toBottomOf="@id/dialog_add_account_item_help"
        android:layout_marginStart="5dp"
        />

    <ImageButton
        android:id="@+id/dialog_add_account_item_help"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_help"
        android:minWidth="0dp"
        android:minHeight="0dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:backgroundTint="@android:color/transparent"
        />

</androidx.constraintlayout.widget.ConstraintLayout>