<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.google.android.material.tabs.TabLayout
        android:id="@+id/cf_news_tab_layout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/cf_news_logo"
        app:tabInlineLabel="true"
        app:tabTextColor="#969696"
        app:tabSelectedTextColor="@color/textColor"
        app:tabIndicatorColor="?attr/colorAccent"
        app:tabMode="fixed"
        />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/cf_news_pager"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/cf_news_tab_layout"
        android:layout_alignParentBottom="true"
        />

    <ImageView
        android:id="@+id/cf_news_logo"
        android:layout_width="20sp"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_cf_logo"
        android:layout_alignTop="@id/cf_news_tab_layout"
        android:layout_alignBottom="@id/cf_news_tab_layout"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"

        />

</RelativeLayout>
